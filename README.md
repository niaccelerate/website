# ni/acc website

The source for [niaccelerate.com](https://niaccelerate.com).

The site is deliberately plain HTML and CSS. Update `index.html` for the text and
`styles.css` for its presentation. Changes merged into `main` are deployed by Vercel.
